# Diplom_1 

# Юнит-тестирование Stellar Burgers 🍔

![Java](https://img.shields.io/badge/Java-11%2B-blue.svg)
![Maven](https://img.shields.io/badge/Maven-3.9%2B-orange.svg)
![JUnit](https://img.shields.io/badge/JUnit-4.13.2-green.svg)
![Mockito](https://img.shields.io/badge/Mockito-4.11.0-purple.svg)
![JaCoCo](https://img.shields.io/badge/JaCoCo-0.8.8-yellow.svg)
![Tests](https://img.shields.io/badge/Tests-35%20passed-brightgreen.svg)

Проект автоматизированного юнит-тестирования для космической бургерной Stellar Burgers. Включает полное покрытие тестов класса Burger с использованием Mockito и параметризации.

## 🛠 Технологии

| Технология | Версия | Назначение |
|------------|--------|------------|
| Java | 11 | Основной язык программирования |
| Maven | 3.9+ | Сборка проекта и управление зависимостями |
| JUnit | 4.13.2 | Фреймворк для модульного тестирования |
| Mockito | 4.11.0 | Создание мок-объектов и стабов |
| JaCoCo | 0.8.8 | Анализ покрытия кода тестами |

## 🚀 Быстрый старт

### Предварительные требования

- Установленная **Java JDK 11** или выше
- Установленный **Maven 3.6** или выше
- Доступ к интернету для загрузки зависимостей

### Установка и настройка

```bash
# Клонирование репозитория
git clone https://github.com/Versaria/Diplom_1.git
cd Diplom_1
# Проверка установки
java -version
mvn -version
```

## 📂 Структура проекта

```
Diplom_1/
├── src/main/java/praktikum/
│   ├── Burger.java                   # Основной класс для тестирования
│   ├── Bun.java                      # Модель булочки
│   ├── Ingredient.java               # Модель ингредиента
│   ├── IngredientType.java           # Перечисление типов ингредиентов
│   ├── Database.java                 # База данных с тестовыми данными
│   └── Praktikum.java                # Основной класс приложения
├── src/test/java/praktikum/
│   ├── BurgerTest.java               # Непараметризованные тесты (14 тестов)
│   ├── BurgerParameterizedTest.java  # Параметризованные тесты (15 тестов)
│   └── BurgerMoveParameterizedTest.java # Тесты перемещения ингредиентов (6 тестов)
├── target/site/jacoco/               # Отчеты покрытия JaCoCo
├── pom.xml                           # Конфигурация Maven с зависимостями
└── README.md
```

## 🧪 Покрытие тестирования

### Непараметризованные тесты (14 тестов)

| Тест | Описание | Ожидаемый результат |
|------|----------|---------------------|
| Установка булочки | Расчет стоимости булочки | Цена = цена булочки × 2 |
| Удаление ингредиента | Уменьшение стоимости бургера | Цена уменьшается на стоимость ингредиента |
| Перемещение ингредиента | Изменение порядка в чеке | Стоимость не изменяется |
| Невалидный индекс удаления | Удаление по несуществующему индексу | `IndexOutOfBoundsException` |
| Невалидный индекс перемещения | Перемещение по несуществующему индексу | `IndexOutOfBoundsException` |
| Получение цены без булочки | Расчет стоимости до установки булочки | `NullPointerException` |
| Получение чека без булочки | Формирование чека до установки булочки | `NullPointerException` |
| Чек без ингредиентов | Только булочки | Корректная структура вывода |
| Чек с одним ингредиентом | Один ингредиент в заказе | Правильное отображение типа и названия |
| Чек с двумя ингредиентами | Несколько ингредиентов | Корректный порядок и форматирование |
| Чек после перемещения | Измененный порядок ингредиентов | Обновленная последовательность в выводе |
| Расчет стоимости | Комплексный расчет | Корректная сумма всех компонентов |

### Параметризованные тесты (21 тест)

**BurgerParameterizedTest (15 тестов)**

| Тест | Описание | Количество тестов |
|------|----------|-------------------|
| Расчет стоимости булочки | Проверка формулы цены | 3 теста |
| Увеличение стоимости | Добавление ингредиента увеличивает цену | 3 теста |
| Отображение в чеке | Ингредиент появляется в финальном чеке | 3 теста |
| Комплексный расчет | Правильность формулы стоимости | 3 теста |
| Наличие названия булочки | Название отображается в чеке | 3 теста |

**BurgerMoveParameterizedTest (6 тестов)**

| Тест | Описание | Количество тестов |
|------|----------|-------------------|
| Сохранение стоимости | Перемещение не влияет на цену | 6 тестов |

## ▶️ Запуск тестов

### Основные команды

```bash
# Запуск всех тестов
mvn clean test
# Запуск конкретного тестового класса
mvn test -Dtest=BurgerTest
# Запуск тестов с детальным логированием
mvn test -Dtest=BurgerParameterizedTest -X
```

### Параметризованный запуск

```bash
# Запуск только непараметризованных тестов
mvn test -Dtest="BurgerTest"
# Запуск всех параметризованных тестов
mvn test -Dtest="*ParameterizedTest"
# Пропуск тестов и только компиляция
mvn clean compile
```

## 📊 JaCoCo отчеты

### Генерация отчетов

```bash
# Запуск тестов и сбор данных для JaCoCo
mvn clean test
# Генерация HTML отчета
mvn jacoco:report
# Просмотр отчета в браузере
open target/site/jacoco/index.html
```

### Структура отчета

- **Обзор** - общая статистика покрытия
- **Покрытие пакетов** - детали по каждому пакету
- **Покрытие классов** - детали по каждому классу
- **Покрытие методов** - детали по каждому методу

## 🔧 Тестируемые методы

| Метод | Описание | Тестовое покрытие |
|-------|----------|-------------------|
| `setBuns()` | Установка булочки | Тесты цены и исключений |
| `addIngredient()` | Добавление ингредиента | Тесты стоимости и чека |
| `removeIngredient()` | Удаление ингредиента | Тесты стоимости и исключений |
| `moveIngredient()` | Перемещение ингредиента | Тесты порядка и стоимости |
| `getPrice()` | Расчет стоимости | Тесты различных комбинаций |
| `getReceipt()` | Формирование чека | Тесты структуры вывода |

## 📝 Примеры использования

### Создание мок-объектов

```java
@Mock
private Bun mockBun;

@Mock
private Ingredient mockIngredient;

@Before
public void setUp() {
    MockitoAnnotations.openMocks(this);
    burger = new Burger();
    
    when(mockBun.getName()).thenReturn("black bun");
    when(mockBun.getPrice()).thenReturn(100.0f);
    when(mockIngredient.getType()).thenReturn(IngredientType.SAUCE);
    when(mockIngredient.getName()).thenReturn("hot sauce");
    when(mockIngredient.getPrice()).thenReturn(50.0f);
}
```

### Параметризованное тестирование

```java
@Parameterized.Parameters(name = "Булочка: {0}, Ингредиент: {3}")
public static Object[][] getTestData() {
    return new Object[][] {
        {"black bun", 100.0f, IngredientType.SAUCE, "hot sauce", 50.0f},
        {"white bun", 200.0f, IngredientType.FILLING, "cutlet", 100.0f},
        {"red bun", 150.0f, IngredientType.SAUCE, "sour cream", 75.0f}
    };
}
```

### Тестирование перемещения ингредиентов

```java
@Parameterized.Parameters
public static Object[][] getMoveCombinations() {
    return new Object[][] {
        {0, 1}, {1, 0}, {0, 2}, 
        {2, 0}, {1, 2}, {2, 1}
    };
}
```

---

<div align="center">

**Проект создан в учебных целях** • [Отчет о покрытии](target/site/jacoco/index.html)

</div>